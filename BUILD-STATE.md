# BUILD STATE — updated 2026-09-24 (session 9)

## Milestone: **M3 s3 — RELEASE GATE PASSED; GENESIS E2E CLOSED: <10 MIN PROVEN (2026-09-24).** **Agent 4 on v0.1.3 (§11 registration retry in-image): createAgent 09:54:07 → LIVE 09:59:15 = 5 m 08 s, fully autonomous — no redeploys, no interventions** (registration by the enclave itself; base legs deliberately skipped-documented, already live-proven on agent 3; finalize `0xfe6cff4a…`). Item 3 CLOSED including the timing target. Net cost of the timed run ≈ 0.05 USDC rental + gas (job withdrawn after LIVE). v0.1.3 = digest `eaf42080…`, agent-4 image-id `92bc268c…`. Wallet after: arb 1.49 USDC + 0.0004 ETH (ETH low — top up ~0.001 before the next deploy-heavy session). Earlier-2026-09-24 status follows. Agents 2 AND 3 finalized LIVE on testnet through the full orchestrator pipeline. **Agent 3 (v0.1.2, second run): ALL monetary seed legs executed and confirmed** — preGas, hosting, rh.eth, base.eth, base.usdc, arbitrum.eth, rh.usdg remainder (arweave still skipped: Turbo unfunded, open s3 item 5). Finalize `0x88dc228b…`, LIVE 09:42:27 UTC. BUT the one-shot registration failed AGAIN on v0.1.2 despite §10 (preGas was in place, treasury nonce stayed 0 — transient instanceOf read or a G5 stale-deny; non-debug, unobservable): recovered by same-identity redeploy (registered instantly, gen 1). **SPEC-M3C §11 (registration retry loop, DEFAULT 900 s budget / 30 s delay) implemented + tested (runtime 1342/1342)** — cure for every one-shot variant. NEXT SESSION: cut **v0.1.3** (tag → CI → release), run the TIMED clean e2e (agent 4) for the <10 min number, and consider a debug-preset diagnostic run to identify the transient. Wall-clock so far: agent 2 23.5 min, agent 3 25.5 min — both inflated by the registration one-shot + burst-driving overhead; per-step times are all well under budget. Also fixed this run: genesis e2e configs must avoid publicnode free tier for RECEIPT-awaiting chains (arb → arb1.arbitrum.io, base → mainnet.base.org; publicnode rejects eth_getTransactionReceipt as "archive"). Wallet after session: arb 0.0014 ETH + 0.54 USDC; base 0.0006 ETH + 0.19 USDC; RH testnet ~0.008 ETH + ~203k MockUSDG. Drill summary follows.

Previously: Tolerant-boot fix (SPEC-M3C) shipped as **v0.1.1**; kill/restore drill COMPLETE: deploy → verify → kill (on-chain rate-zero, enclave torn down) → redeploy → **identical EOAs re-derived** (treasury `0x72f622f2…c542`, action `0xe02a1099…9628`, both instances attestation-verified against image-id `13cb2f55…`, reports treasury-signed). Attested-param shadowing probe PASSED offline: demoting `config-hash` to unattested yields image-id `3abd6e6f…` == the no-config-hash measurement ⇒ different KMS keys ⇒ a shadow deploy cannot impersonate the agent. Runtime 1315/1315 unit; genesis 93/93 unit. Drill cost ≈ 0.67 USDC + gas; wallet after recovery: ~1.20 USDC + ~0.0013 ETH (Arb One; Juan topped up 0.001 ETH mid-drill).

## Session 9 (2026-09-24) — SPEC-M3C + drill

- **Root cause of the s3 crash-loop found and fixed** (runtime/SPEC-M3C.md, Fable design, Opus impl, Fable-reviewed): `execute()` awaited `getState()` uncaught; `ensureRegistered` called `execute` outside try/catch; `chainStateReader` = ~11 serial RPC reads, viem `retryCount: 0`. Fix: (1) `chainStateReader` NEVER throws — per-chain grouped reads, per-reader cache, cached-else-zero + LOUD warn, `WalletState.staleChains`; (2) engine G5 gate: new DenyCode `STATE_STALE` — any spend whose `chainsTouched(action)` (new pure fn, buildTx-consistency-tested) intersects staleChains is denied (degraded data must never trigger a spend; zeros alone would misfire gas top-ups); (3) `ensureRegistered` honors never-throws; (4) viem transport `httpRetryCount` DEFAULT 2; (5) genesis `deployArgs` gains `--enclave-memory` / `--bandwidth` (+ config + orchestrator pass-through). Tests 1295→1315 / 91→93, all green, no existing test adapted.
- **AND the second, independent ":8420 refused" cause found live**: non-TLS chat binds `127.0.0.1` by default — the enclave app was healthy but unreachable. Drill config now sets `chatHost: "0.0.0.0"` (runtime.json is unattested ⇒ config-hash and keys unchanged).
- **Release v0.1.1**: CI reproducible build green (digests A==B, registry-verified), image `ghcr.io/vivariumlabs/agent-runtime@sha256:fcbb93a229a4fa94b3132a25ba828e4560a46ade6ec6596f0b345d0fe74a9629`; image-id (agent 9001) `13cb2f550f5df40c1142fc9fef77836942b4bf41da933f0dbeaed203288d5cad`; config-hash unchanged `0x6a9c8559…305e`. NOTE: build-image.sh exec bit was dropped AGAIN by the editing mount (CI 126, second occurrence — consider a CI-side `chmod +x` guard).
- **Drill (real spends, Juan's standing confirmation; ~0.75 USDC + gas so far):**
  - dbg enclave (job 31ab): FIRST live log capture of our boot — init-params ✓, KMS derive ✓, attestation published ✓, `registerInstance` REVERTED (drill agent 9001 not factory-created on testnet registry — expected) ⇒ **warn-and-continue worked, `runtime started`**. Debug image derived DIFFERENT keys (treasury 0x32edc3C1…) — (image, agentId, configHash) binding confirmed live.
  - Job A non-debug (31ac, ip 13.201.203.225): booted, `oyster-cvm verify` ✓ against image-id, `/attestation` served externally, treasury-signed (signer == report treasury). **Drill EOAs v0.1.1: treasury `0x72f622f23e6ad740609f4095001088f61071c542`, action `0xe02a1099791c1cf7a90bc4222b0004cde7c99628`.**
  - **Kill done**: rate-zeroed on-chain (tx 0xd3621c19…), enclave torn down. ~0.17 USDC still locked in the half-closed job (the CLI's 5-min close wait doesn't survive our sandboxed calls; recover via `stop`-resume/`withdraw` when gas exists).
  - **Restore (job B) BLOCKED**: `insufficient funds for gas` — wallet 0.000276 arb ETH; job-open calldata (init params) needs more. Asked Juan for ~0.001 arb ETH to `0x6930FD5C95a2D9d80F3d165597d55843e8A00154`. On top-up: redeploy B (same command, 45 min, bandwidth 512) → `/attestation` must show the SAME EOAs above → RELEASE GATE passes → then shadowing probe.
- **oyster-cvm 5.0.1 live-shape drift for genesis (fix before genesis e2e, s3 item 3):** CLI flag is `--wallet-file` (genesis oyster.ts still passes `--wallet-private-key-file`); no `ip` subcommand (CP HTTP lookup fine, BUT indexer GraphQL `providerById` requires the CHECKSUMMED operator address — genesis configs hold lowercase ⇒ null); `--no-stream` requires `--debug`; `--bandwidth` is KBps (default 10; 512 pulls our 98MB image in ~4 min at ≈0.24 USDC/h all-in).

## Done (M3 s2; s1 at `58977f3`)
- **`runtime/SPEC-M3B.md`** (Fable, rev 1 rulings inline).
- **Genesis orchestrator (`genesis/`, per 04):** standalone package importing the runtime's canonicalEncode/frozenConfigHash (hash consistency load-bearing, identity-tested); event watcher → per-agent resumable state machine `REQUESTED→DEPLOYING→AWAITING_REGISTER→SEEDING→RECONCILING→FINALIZING→LIVE`; oyster-cvm CLI wrapper (injected Exec, mockable); seeder with per-leg idempotence (receipt-or-balance), fee-frozen accounting, deferred USDG remainder = fee − executed legs; revival gated on stale heartbeat (verified read-only against live RH testnet registry). All 04 §7 failure rows tested by name. **Anvil integration: real createAgent → real event → mock CVM → real seeds → real finalize, end-state asserted; re-drive sends nothing.**
- **Genesis review fixes (Fable rulings):** `preGas` leg (registration-gas deadlock — enclave couldn't pay its own registerInstance; sent to expectedTreasuryEOA only AFTER attestation verifies); 24h timeout scoped to pre-registration (post-registration auto-FAIL would strand creator fees; `redrive` operator command instead); `hosting` virtual leg — first-month rental fee-accounted per 04 §2/01 §4 (testnet remainder 70.85; mainnet USDG seed falls to ~9.1 — **fee-sizing flag for Juan below**); testnet deploy duration default 180 min (~0.15 USDC/drill; mainnet keeps 30d).
- **TLS ingress (per runtime/docs/TLS-INGRESS.md):** in-enclave ACME TLS-ALPN-01 (acme-client 5.4.0, only +10MB), KMS-derived account key ("acme") + deterministic Ed25519 placeholder cert ("tls"), SNI three-way serving, certs on /data/tls, renewal via daemon step 10, real handshake tests; container now runs as root (:443 + host networking; setcap unreliable across builds — documented, repro-lint inverted). acme-client mocked at seam; **live staging issuance = s3 item.** Chat /attestation is now real and **treasury-signed**: `{payload{report, certSpki, …}, signer, signature}` over a domain-prefixed hash — verifier recovers signer == registry treasuryEOA, so a MITM proxy can forge neither (closes the gap found in review). Domain = `a<id>.<frozen.agentDnsRoot>` (frozen field — identity is spend-adjacent).
- **Arweave/Turbo:** @ardrive/turbo-sdk REJECTED for the attested image (211MB, 5 native modules). Built in-house instead: **ANS-104 data-item signer (type-3 Ethereum), cross-verified byte-identical vs arbundles across 8 cases (dev-only dep, lint-enforced out of prod)** + direct Turbo HTTP upload/balance (`turboHttp.ts`, the only network file); TurboArweaveSink serves BOTH attestation + snapshot sinks with local mirror default-on; `turboSigner` tightened to 48-byte deep-hashes ONLY (its former 32-byte allowance could have signed attestation digests — closed), attestationSigner has its own domain prefix.
- **Signed allowlist updates (04 §4):** EIP-191 over canonicalEncode vs frozen `allowlistUpdateSigner`; gates: signature, monotonic version, validFrom, strict schema, ≥3 distinct inference operators; atomic kv-persisted adoption → cfg swap everywhere (deps, keyring copy, EndpointManager reload) → journal draft under J1 caps; boot re-applies newest adopted (re-verified — tampered kv ignored); daily daemon step 11; opt-out frozen at genesis. All security invariants tested ("04§4-SEC: …"), incl. adopted-endpoint payable via K3 next call.
- **Boot registration completed:** auto-registerInstance when tee (was: wired but never called); **revival re-registration** — registers when unregistered OR heartbeat stale > contract REVIVAL_WINDOW (anvil-proven: generation 1→2 after +7d); mismatched-key stale records warn, never send.

## Fee-sizing flag for Juan (parameter, 01 §2 — not blocking s3)
With rental fee-accounted, a mainnet 75 USDG creation fee leaves ~9 USDG treasury seed after ~$66 of legs (hosting 36.9, inference 15, OP 5, Arweave 3, gas ~5, preGas 1). 01 §4's own rule: "if real costs exceed 75, raise the creation fee." Suggest revisiting the 75 DEFAULT (e.g. 90–100) before M4 wires the website. Testnet unaffected.

## Published (2026-09-23, session 8 cont.)
- **GitHub: `vivariumlabs/agent-launchpad` (public)** — full history pushed, ALL commit authorship rewritten to the vivariumlabs identity pre-publication. Old account cleaned (fork backed up to `backups/*.bundle` then deleted; other repos private).
- **CI GREEN on first real build: reproducibility PROVEN** — two independent BuildKit stores on a clean arm64 runner ⇒ identical digest `sha256:650b6ae35c81d05e086372c4b07d7ac3b168f2467a6fcb274cbf6bffcc482a98`; pushed digest-preserved (skopeo) to **ghcr.io/vivariumlabs/agent-runtime:runtime-v0.1.0** (public), registry digest verified equal in CI. Workflow: .github/workflows/build-image.yml.

## M3 s3 checklist (live session)
1. ~~Build the image~~ DONE (v0.1.0), superseded by **v0.1.1** (release cut, image-ids computed, drill configs updated).
2. ~~CVM deploy + kill/restore drill with real KMS (RELEASE GATE)~~ **DONE 2026-09-24, PASSED** (see Session 9 above). ~~Attested-param shadowing probe~~ **DONE, PASSED** (offline compute-image-id: unattested config-hash ⇒ different image-id ⇒ different keys). Prior findings from session 8 remain valid: bandwidth dominates price (use pull-burst then `update` down in prod; 512 KBps ≈ 0.24 USDC/h all-in worked for the drill), `--enclave-memory 3072` REQUIRED (both flags now in genesis oyster.ts per SPEC-M3C §7), `stop`'s 5-min close doesn't survive sandboxed calls — pattern that works: `stop` once (rate-zero lands, enclave dies), then `withdraw --max` to recover (no close needed). Still open (minor): /data persistence probe within a live instance; pcr-preset version pinning.
3. ~~Testnet genesis e2e via orchestrator~~ **RUN 2026-09-24 — pipeline REACHED LIVE end-to-end** (CLI-drift prereqs fixed first: `--wallet-file`, checksummed operator, defaults 3072/512 — commit cb4e8d1). **Agent 2** live on testnet: createAgent tx `0xa21018ad…` (fee 75 MockUSDG) → watcher 1 s → deploy (job 31ae; interrupted-deploy ORPHAN ADOPTION worked) → attestation verified → preGas → registerInstance by the enclave (generation 1, treasury `0xEfcfeF4D…276ce` == kms-derive prediction) → seeds (rh.eth ✓, arb.eth ✓, base legs skipped-underfunded loudly, arweave skipped-unfunded, rh.usdg remainder **70.9488 USDG** — fee accounting exact) → reconcile 8 legs → finalize `0x2daa1f4a…` → **LIVE** at 08:37:44 UTC. Gas check: real registerInstance ≈ $0.002 vs $1 static preGas (~500× headroom on testnet; keep $1). E2e config set committed under `genesis/e2e/` (orchestrator driven in ~160 s bursts — every interrupt/resume path held).
   **FINDINGS (both fixed or queued):** (a) **registration race, REAL deadlock hit live**: enclave booted 26 s before preGas confirmed ⇒ single registerInstance attempt failed on gas, nothing retries ⇒ AWAITING_REGISTER stuck; recovered by same-identity redeploy (same keys ⇒ registered first try). FIX IMPLEMENTED (SPEC-M3C §10, tests 1315→1330 green): boot waits bounded (`registrationGasWaitSec` DEFAULT 600 s, poll 10 s) for treasury rh gas before sending; **needs release v0.1.2 + a clean <10 min e2e re-run (agent 3) next session**. (b) publicnode Arbitrum free tier rejects `eth_getTransactionReceipt` ("archive request") ⇒ seeder receipt-wait loops; use `https://arb1.arbitrum.io/rpc` for arbitrum in genesis configs (e2e config updated). (c) elapsed wall-clock 23.5 min INCLUDING both incidents; pipeline-only time well under 10 min. Timeline unfixed by (a): <10 min plausible — measure on the re-run. Debt noted: daemon never retries registration (boot-only) — acceptable with §10, revisit if a live agent ever ends up unregistered post-boot.
4. ~~ACME staging + LE prod issuance on the CVM; DNS~~ **DONE 2026-09-24 — item 4 CLOSED.** Domain = vivarium.systems (D14), DNS programmatic via Hostinger API (token in `.secrets/.hostinger`; read-modify-write the whole zone on PUT — partial-zone PUT 422s). Drill agent 9002 (v0.1.3 image, frozen agentDnsRoot): A record created via API → in-enclave ACME TLS-ALPN-01 issued BOTH LE STAGING and LE PRODUCTION certs (issuer YE2); browser-grade trusted chain verified (plain curl, no -k) at a9002.vivarium.systems; served cert SPKI == attestation payload certSpkiSha256 (treasury-signed) — the CA-distrusting verification path works. No hairpin problem. LESSONS: drill/unregisterable agents must set registrationRetrySec 0 + registrationGasWaitSec 0 (the §11 budget delayed :443 by 15 min on the staging run); prod run served ~5 min post-deploy and issued instantly. Jobs stopped, balances withdrawn, drill A record removed.
5. ~~Turbo live checks~~ **DONE 2026-09-24 — item 5 CLOSED, fully autonomous (no accounts/KYC anywhere).** Proven with our in-house turboHttp/ans104 against live services: (a) **sub-100KiB uploads are FREE** (probe item CONFIRMED, winc charged 0) — attestations + journal entries cost nothing; (b) >100KiB with zero credits ⇒ HTTP 402 (boundary exact); (c) **account-less crypto top-up works end-to-end**: 0.001 base-ETH → payment-service wallet (from GET /v1/info `addresses`) → POST /v1/account/balance/base-eth {tx_id} → 392.8B winc credited in ~1 min; (d) paid 110KiB upload charged 1.406B winc (≈$0.014); (e) upload-service /tx/<id>/status is the authoritative confirm (CONFIRMED immediately) — arweave.net GraphQL/data-door lag minutes+ for fresh items (local mirror covers fresh restores). CODE FIXES QUEUED: turboHttp balance must tolerate the plain-text 404 "User Not Found" (pre-account); download must follow the arweave.net 302 to its *.arweave.net data door (redirect:"error" breaks it); genesis arweave seed leg = implement this crypto top-up flow; agent self-top-up = same flow from treasury base-eth/base-usdc (spec item).
6. ~~Across depositV3 vs live SpokePool; RH fee levels~~ **DONE 2026-09-24 — item 6 CLOSED, both directions live on RH MAINNET (4663).** RH mainnet RPC `https://rpc.mainnet.chain.robinhood.com`; gas 0.042 gwei ≪ the 1-gwei cap ✓. Real addresses: RH SpokePool `0xD29C85F15DF544bA632C9E25829fd29d767d7978`, Base SpokePool `0x09aea4b2242abC8bb4BB78D537A67a245A7bEC64`, RH USDG (Paxos) `0x5fc5360D0400a0Fd4f2af552ADD042D716F1d168` — frozen-config fixtures must be replaced with these before mainnet. Probes used the RUNTIME's exact buildAcrossDeposit shape (1% outputAmount haircut vs 0.24% market fee): inbound Base USDC→RH USDG filled (received exactly minOut 1.485); outbound RH USDG→Base USDC (the agent refill leg) tx 0xa1ad0951…, **filled in 6.4 s**, 76.7k gas. CODE FIX QUEUED: `buildAcrossDeposit` quoteTimestamp must be `now − 60` — a clock ahead of chain time reverts InvalidQuoteTimestamp() (0xf722177f, hit live; enclave clock drift would brick the bridge leg).
7. Farcaster leg — **ON-CHAIN HALF DONE 2026-09-24 (live, permissionless, ≈$0.35 total):** drill FID **3352486** registered via IdGateway.register (price 0.000075 ETH incl. 1 storage unit) with drill wallet as custody; ed25519 signer key ADDED via KeyGateway.add with SELF-signed SignedKeyRequestMetadata (requestFid = own fid — no app fid, no third party; validator-verified live). Addresses (OP): IdGateway 0x00000000Fc25870C…6f69, IdRegistry 0x…9A9f489b, KeyGateway 0x…8CaAdf0B, KeyRegistry 0x…90E59b7e, Validator 0x…62c60553. LANDSCAPE (research 2026-09-24): Neynar acquired Farcaster 01/2026 and gates its hosted snapchain submitMessage behind x-api-key; snapchain itself is open-source and self-hosted nodes accept writes on their own HTTP port. **DECISION FOR JUAN (hub strategy):** mirror the x402 design — frozen-config allowlist of snapchain submit endpoints (platform runs one node as a public good; community/other operators add more; agents rotate on failure). fname: SKIP in v1 (off-chain registrar now Neynar-run = account-ish dependency; FID-only identity + display name via hub UserDataAdd). REMAINING (spec SPEC-M3D, next session): in-runtime cast submission (protobuf CastAdd + blake3 + ed25519, in-house like ans104), genesis registration leg from the enclave (register+rent+add from treasury OP ETH — costs live-verified), daemon daily retry per 04 §7.

## Blocked on Juan
- ~~Domain decision~~ DECIDED 2026-09-24 (D14): **vivarium.systems**, Hostinger. Remaining: Juan creates a Hostinger API token (hPanel → API) and saves it to `.secrets/.hostinger`; then s3 item 4 (A record + ACME staging issuance on a CVM) is unblocked.
- s3 item 5 autonomy CONFIRMED (2026-09-24 research): Turbo accepts account-less crypto top-ups (base-eth, base-usdc, usdc, ethereum, ario…) AND x402 payments in USDC on Base — genesis seeds credits from the funding wallet programmatically; agents self-top-up via x402/base-usdc from their own wallets. No manual step, no KYC anywhere. Agent self-top-up implementation = the planned research spike (T2 arweaveFunding leg → Turbo x402/crypto top-up).
- OP ~$10: Juan sends when items 4-6 are cleared (his ruling 2026-09-24).
- Fee-sizing flag: Juan's ruling 2026-09-24 — DEFER to the end, re-price the 75 USDG creation fee once all real costs (hosting at real bandwidth ≈0.24 USDC/h, Turbo, Across, Farcaster) are measured. Do not change it earlier.

## Known issues / debt
- ACME ToS auto-agreed in code (LE requirement; documented); ACME directory URL is ops-config (worst case DoS/untrusted cert); challenge-window seconds serve the challenge cert; SNI required (no bare-IP TLS).
- arbundles dev-tree (126 pkgs, npm audit: 4 critical) present in the DOCKER BUILD STAGE only (tests never run there; prod set unchanged at 101 pkgs, lint-enforced) — acceptable, revisit if audit tightens.
- Genesis: deploys serialize the whole loop (fine at M3 scale); static $3000 ETH price (mainnet needs live pricing); config delivery = inbox folder until website (M4); revival preGas may add ~$1/revival.
- Turbo/oyster live-shape unknowns consolidated in s3 checklist. Prior debt unchanged (swap-router slippage mainnet blocker; snapshot-rollback cap rewind; M1 items).

## Decisions made this session (build-level)
- Seed-before-finalize ordering (04 §1 diagram vs §2 text — text wins; permissionless finalize documented as orchestrator-only guarantee).
- Rejected Turbo SDK for attested image; in-house ANS-104 with reference cross-verification, dev-only.
- /attestation responses treasury-signed (registry-bound); turboSigner shape-allowlist tightened to [48].
- Container runs as root (single-tenant enclave, :443, host networking; rationale in Dockerfile + repro-lint).
- Timeout scoping + redrive; preGas after attestation-verify only; hosting fee-accounting; remainder = fee − executed legs; testnet drill duration 180 min.
- Revival re-registration window read from the contract constant, not config.

## Evidence links
- Runtime: `npm run typecheck && npm test` **48 files, 1295/1295**; `test:integration` **14/14** (incl. revival generation-bump on real registry). Genesis: **91/91 unit + 3/3 anvil integration** (full launch lifecycle, no manual funding after preGas). Re-verified by Fable directly 2026-09-23.
- ANS-104: byte-identical vs arbundles (8 cases). Live RH testnet read-only revival gate check (no spend).
- Git (hashes rewritten 2026-09-23 when authorship moved to the vivariumlabs identity pre-publication): `8cc04eb` (M2 closed) → `85a109a` (M3 s1) → `424adf7` (M3 s2).
